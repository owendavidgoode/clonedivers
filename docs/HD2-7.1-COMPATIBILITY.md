# HD2 7.1 compatibility audit — September 23

Installed Steam build: 25327279. This audit is read-only for the game and public
release. Raw current resources are under `dist/compat-7.1/current/`; comparison
results are in `dist/compat-7.1/audit.json`.

The text/audio rebase is complete in the local candidate. **Gameplay acceptance
is still required before release.** Automated checks do not establish audible or
visual behavior in the game.

## Completed rebase

Final candidate: `dist/player-update-2026-09-23/feed-7.1-final/manifest.json`.
Audio: sibling `audio-7.1-v4/`, including build and independent serialization
verification reports. Earlier candidate feeds/audio attempts are superseded.

- Both final label banks contain all 3,246 current keys: 287 restored. The merge
  retains themed names/flavor and rank titles, keeps current mechanics/item text
  instead of stale placeholders, blank weapon names and obsolete descriptions,
  and applies eight preset-specific voice labels. Decisions are recorded in
  `feed-7.1-final/labels/merge-report.json`. Stock armor names remain intact.
- Rebuilt 62 PEW-PEW bank occurrences (60 unique banks), five generic clone banks,
  and three later overrides that otherwise would replace the refreshed banks:
  RiqCrow cruiser/LAAT's autocannon bank (164), Probe Guard Dog (177), AT-TE (227).
  All 70 rebuilds preserve current non-media chunks byte-for-byte, including
  HIRC events, routing, timing and properties. Matching embedded recordings are
  carried by media ID; new-only media remains current; obsolete IDs are omitted.
  Current volume properties take precedence over old blaster-bank property edits;
  this needs an audible sustained-fire check, not a claim of identical mixing.
- Generic clone external recordings remain unchanged. Embedded voice samples
  needed by current streamed sources are supplied as streams, preserving the
  current routing. Shared exertions retain available clone replacements while
  current-only shared samples are retained.
- All four RC banks are current-bank bytes with only the 1,525 mapped codec edits.
  All 1,525 expanded RC stream payloads remain byte-identical, retaining the same
  496 distinct recordings. No Boss recording gain/normalization was applied.
- Independent serialized verification matches 64 nonempty source parts to the
  published manifest, checks all 70 banks, and preserves 3,870 existing external
  stream occurrences. All 16 launcher selection combinations and native launcher
  tests pass. Load-order inventory resolves every selected Full/clone main file;
  the three relevant late winners above are included in the rebase.

Tools: `tools/rebase-player-audio.py`, `tools/check-player-audio-rebase.py`, and
`tools/build-player-update.py` (`--current-strings` and `--audio-rebase` required).
Source extraction uses Filediver v0.7.53 on Steam build 25327279. This is a targeted
player/weapon-bank refresh, not a rebase of every unrelated music/NPC bank.

Still needed in game: scopes/ADS, combined armor and four helmet targets, Delta
off/on and teammate playback, voice interruption, sustained automatic fire,
MTT cannon alignment, and Full/Lighter behavior. No game files or public feeds
were changed by this staging work.

Subsequent authorized local test install: 1,124 Full-profile target files verified
at 21:21 UTC September 23, Delta/droids/cannons enabled. Used the launcher's tested
inventory/plan/apply logic through `tools/PlayerUpdate.Apply`; 96 old files were
parked without overwriting existing backups. The settings/receipt/manifest backup
and install result are in `dist/player-update-2026-09-23/local-install/`. Steam
launch requested; no runtime success is claimed and the public feed is unchanged.

## Original findings (resolved in staging)

1. **Refresh the winning English text bank.** Current base has 3,246 keys; the
   staged voice-label bank has 2,959 and omits 287 current keys. The September 12
   base extraction had 3,194 keys: 52 additions since that extraction, one changed
   shared value and no removed keys. Thus some omissions were inherited from the
   older Galactic Map text template, not introduced specifically by 7.1. Merge
   intended mod text into current game text, preserving new keys, then apply the
   eight voice-label edits. The eight original voice-name keys remain unchanged.
   This needs coverage for both generic clone and Delta presets, and for the live
   pack's winning map/label overrides.
2. **Rebase voice-bank overrides.** All four current actor banks differ from the
   shipped clone template and from the September 12 extracted vanilla banks.
   Current HIRC chunks also differ from those historical extractions; differences
   are not merely the outer 16-byte archive wrapper. Do not assume all differences
   arose in 7.1 rather than an intervening update or mod authoring.
   All 1,525 mapped RC media IDs still exist as streamed Vorbis sounds in their
   expected slots; bank version remains 154. Retain the recordings/mappings and
   rebuild required codec edits on current bank structures. Preserve generic clone
   fallbacks separately instead of copying the stale whole-bank template forward.

## Targeted follow-up checks

- [Arrowhead's 7.1 notes](https://arrowhead.zendesk.com/hc/en-us/articles/30492249897756-Devoid-of-Liberty-7-1-0)
  change automatic fire audio/timing and voice cancellation. Audit the blaster
  sound overrides too; then test sustained fire and interrupted voice lines.
  The Clone Blasters geometry ZIP has no bank overrides, but the actual weapon
  sound pack, PEW-PEW Republic, includes 62 bank entries (60 distinct bank IDs),
  along with streams/dependencies. Its whole-bank overrides require a compatibility
  review; they are not merely replacement audio samples. No specific PEW bank
  regression has yet been demonstrated.
- The notes fix armor changes not appearing to other players. Test the combined
  roster with a teammate, including helmet/body swaps. This is not evidence that
  cosmetic asset IDs need changing: the four chosen helmet consumers remain unique
  in current extractor metadata, and all 90 scope archive targets remain present.
- Enemy spawn allocation and defense-mission waves changed. Old combat captures
  cannot isolate mod performance across this update. Compare Full/Lighter on the
  same current game version and similar encounters.
- No confirmed 7.1-specific scope, intro or PC FOV patch is identified. The existing
  scope repair and War Strider visibility work remain separate mod issues.

The patch notes do not prove that stale bank overrides defeat a specific engine
fix. Rebuilding against current resources avoids that unverified assumption; the
actual sound behavior still needs an in-game check.

## September 24 — 7.1.1 targeted comparison

Installed Steam build 25480438 started successfully with the combined preview pack.
Filediver extracted 133 current resources for comparison: every one of the 65 rebased
Wwise banks, the English string resource and the Watcher audio bank matched its prior
base byte for byte. The comparison receipt is local at
`dist/compat-7.1.1/comparison.json`. No further audio/text rebase is needed for r10.
This does not establish every model's gameplay behavior. ADS acceptance was on the
prior 7.1 candidate; Supply FRV and Watcher behavior remain experimental, explicitly
approved for inclusion by the owner.
